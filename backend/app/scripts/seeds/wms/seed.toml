name = "wms"
depends = ["business-admin"]

# WMS-specific menu, role, dictionary, and sample data files live in the product
# seed pack. Tables with the same name are appended after dependency packs.

[[tables]]
table = "platform_menu"
file = "platform_menu.json"
